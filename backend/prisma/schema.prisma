generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Testing {
  id   Int    @id @default(autoincrement())
  name String @map("Name")

  @@map("test")
}